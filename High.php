<?php
class High {

    public $number = mt_rand(1, 100)
    public static function attempt()
    {
        $log = new Log();

        if ($user == self::$number){
            echo ("That is correct");
        }   elseif ($user > self::$number){
            echo ("lower");
        } else lower
                $log->logError("User $username failed to log in!");
                return false;
            };
        
    }
    }
}